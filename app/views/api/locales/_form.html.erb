<%= form_for(api_local) do |f| %>
  <% if api_local.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_local.errors.count, "error") %> prohibited this api_local from being saved:</h2>

      <ul>
      <% api_local.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
